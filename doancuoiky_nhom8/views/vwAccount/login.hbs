<!DOCTYPE html>
<html lang="en">
<head>
    <title>Đăng nhập</title>
    <link rel="icon" href="img/icon.png">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
    <style>
        :root {
            --color: #eec438;
            --color-word: #000;
        }

        body {
            background: var(--color);
            font-family: arial;
            height: 100vh;
            display: flex;
        }

        .login-form h1 {
            font-size: 36px;
            text-align: center;
            color: var(--color-word);
            margin-bottom: 30px;
            font-weight: normal;
        }

        .login-form .social-icon {
            width: 100%;
            font-size: 20px;
            color: var(--color-word);
            text-align: center;
            float: left;
        }

        .login-form {
            background: #fff;
            width: 450px;
            border-radius: 6px;
            margin: auto auto;
            display: table;
            padding: 15px 30px 30px;
            box-sizing: border-box;
        }

        .form-group {
            float: left;
            width: 100%;
            margin: 0 0 15px;
            position: relative;
        }

        .login-form input {
            width: 100%;
            padding: 5px;
            height: 56px;
            border-radius: 74px;
            border: 1px solid #ccc;
            box-sizing: border-box;
            font-size: 15px;
            padding-left: 75px;
        }

        .login-form .form-group .input-icon {
            font-size: 15px;
            display: -webkit-box;
            display: -webkit-flex;
            display: -moz-box;
            display: -ms-flexbox;
            display: flex;
            align-items: center;
            position: absolute;
            border-radius: 25px;
            bottom: 0;
            height: 100%;
            padding-left: 35px;
            color: #666;
        }

        .login-form .login-btn {
            background: var(--color);
            padding: 11px 50px;
            border-color: var(--color);
            color: var(--color-word);;
            text-align: center;
            margin: 0 auto;
            font-size: 20px;
            border: 1px solid var(--color);
            border-radius: 44px;
            width: 100%;
            height: 56px;
            cursor: pointer;
        }

        .login-form .reset-psw {
            float: left;
            width: 100%;
            text-decoration: none;
            color: var(--color-word);
            font-size: 14px;
            text-align: center;
            margin-top: 11px;
            font-style: italic;
        }

        .login-form .reset-psw:hover {
            color: #666666;
        }

        .login-form .social-icon button {
            font-size: 20px;
            /*color: var(--color-word);;*/
            color: cornflowerblue;
            height: 50px;
            width: 50px;
            /*background: var(--color);*/
            background: #fff;
            border-radius: 60%;
            margin: 0px 10px;
            cursor: pointer;
        }

        .login-form .social-icon .bg-email {

            border: 1px solid #e86464;
            background: #e86464;
        }

        .login-form .social-icon .bg-email:hover {
            background: #f14646;
        }

        .login-form .social-icon .bg-fb {
            background: #5c5cce;
            border: 1px solid #5c5cce;
        }

        .login-form .social-icon .bg-fb:hover {
            background: #6262e3;
        }

        .icon-mail, .icon-fb {
            color: #fff !important;
        }

        .login-form button:hover {
            opacity: 0.9;
        }

        .login-form .seperator {
            float: left;
            width: 100%;
            color: var(--color);
            border-top: 1px solid var(--color);
            text-align: center;
            margin: 50px 0 0;
        }

        .login-form .seperator b {
            color: var(--color-word);
            width: 40px;
            height: 40px;
            font-size: 12px;
            text-align: center;
            line-height: 40px;
            background: #fff;
            display: inline-block;
            border: 1px solid var(--color);
            border-radius: 50%;
            position: relative;
            top: -22px;
            z-index: 1;
        }

        .login-form p {
            float: left;
            width: 100%;
            text-align: center;
            font-size: 16px;
            margin: 0 0 10px;
        }

        .err_login p {
            color: red;
            font-style: italic;
        }

        .let_register {
            color: #5454ef !important;
        }

        .let_register:hover {
            color: #81b9e7 !important;
        }

        @media screen and (max-width: 767px) {
            .login-form {
                width: 90%;
                padding: 15px 15px 30px;
            }
        }
    </style>
</head>
<body>

<div class="login-form align-items-center">
    
    <!--    <form>-->
    <h1>Đăng nhập</h1>
    <form action="/login" method="post">
        <div class="form-group">
            <input type="text" name="username" placeholder="Username" {{#if username}}
                value="{{username}}"
            {{/if}} autofocus required>
            <span class="input-icon"><i class="fa fa-user"></i></span>
        </div>
        <div class="form-group">
            <input type="password" name="password" placeholder="Password" required>
            <span class="input-icon"><i class="fa fa-lock"></i></span>
        </div>

        {{#if err_message}}
            <div class="err_login">
                <p><i class="fa fa-user-times" aria-hidden="true"></i> {{err_message}}</p>
            </div>
        {{/if}}

        {{#if loginFailed}}

            <div class="err_login">
                <p><i class="fa fa-user-times" aria-hidden="true"></i> Email được liên kết với tài khoản {{provider}} của bạn đã dùng để đăng nhập với một tài khoản đã có trong hệ thống. Vui lòng sử dụng tài khoản khác</p>
            </div>
        {{/if}}

        <button class="login-btn">Đăng nhập</button>

        <a class="reset-psw" href="/forgot-password">Quên mật khẩu?</a>
        <div class="seperator" ><b style="color: #666666;">hoặc</b></div>
        <!--        <p style="color: var(--color-word); font-style: italic">Đăng nhập bằng phương thức khác</p>-->
    </form>
    <div class="social-icon d-flex justify-content-center">
        <form action="/auth/google" method="post">
            <button class="bg-email" type="submit"><i class="icon-mail fa fa-google"></i></button>
        </form>
        <form action="/auth/facebook" method="post">
            <button class="bg-fb" type="submit"><i class="icon-fb fa fa-facebook"></i></button>
        </form>

    </div>
    <p class="d-flex"><a class="reset-psw let_register" href="/register">Đăng ký ngay?</a><a
            class="reset-psw let_register" href="/">Về trang chủ?</a></p>


    <!--    </form>-->
</div>
</body>
</html>